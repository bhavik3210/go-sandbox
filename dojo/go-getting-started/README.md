`go mod init github.com/bhavik3210/go-project`

`go run github.com/bhavik3210/go-project`

`go run .` there is "main" package in current dir and run that 

`go build` generates an executable that can be run